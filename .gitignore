# Build artifacts
/target/

# Runtime data — local/ is for personal/private campaigns, never committed
glorfindel-data/local/
# Legacy per-campaign ignores (kept for safety on older checkouts)
glorfindel-data/campaigns/shire/
glorfindel-data/campaigns/Claude playtime/
